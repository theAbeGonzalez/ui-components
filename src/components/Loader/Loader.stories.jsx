import React from 'react';
import Loader from './Loader.component';

const storyConfig = {
  title: 'Loader',
  component: Loader,
};

const loader = () => <Loader />;

export { loader };

export default storyConfig;
